/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // images: {
    //     unoptimized: true,
    // },
};

// Merge MDX config with Next.js config
export default nextConfig;
